
# SESSION UPDATE 2026-08-27 ~12:30 (pass 6 SHIPPED)
- master = app.json-metro commit (after 3b269be "Home pass 6..."), gh-pages = "Add .nojekyll (Jekyll was excluding _expo/)".
- LIVE: https://useeman32-design.github.io/deenapp/ — bundle entry-3c21e9d2b46ce0a9713691f3214fe91b.js curl-verified (200, 2.1MB; greps: Daily Videos x2, Accounts to Follow, DAILY AYAH/HADITH, Tap to share, Open in YouTube, campaign-quran, scholar-1; FA ttf + campaign/scholar imgs 200).
- CRITICAL infra findings this session (sandbox resets wipe node_modules, /tmp, /usr libs, .git/config+remotes, git identity):
  * export-web.sh REBUILT & COMMITTED: expo export --output-dir dist/deenapp + `node scripts/slashguard.mjs` + node --check. Now emits `touch dist/deenapp/.nojekyll` (Jekyll EXCLUDES underscore dirs like _expo/ without it — this 404'd the whole JS after a wipe+cp deploy that lost .nojekyll).
  * scripts/slashguard.mjs (COMMITTED): idempotent; prefixes uri:"/", src="/", href="/", exports="/assets/... (FA fonts!), appendBaseUrl defaults (n="" in new SDK, n="/" old), AND patches extractExpoPathFromURL to strip leading /deenapp/ — WITHOUT that patch the new SDK router 404s ("Unmatched Route") because it never strips the base; anchor hrefs were relative (leading-slash-stripped) causing /deenapp/deenapp doubling.
  * app.json web now needs "bundler": "metro" (new SDK: "No platforms are configured to use the Metro bundler").
  * chromium: npx playwright-core install chromium-headless-shell (build 1234); /usr/lib NOT writable — libs persist at /home/user/.chromium-libs/usr/lib/x86_64-linux-gnu with soname symlinks created (libXdamage.so.1, libasound.so.2, libatk-1.0.so.0, libatk-bridge-2.0.so.0, libatspi.so.0, libxkbcommon.so.0); run shots with LD_LIBRARY_PATH=that dir. /tmp/serve = symlink -> dist (recreate after resets).
  * git: remote+identity lost on reset -> re-add `git remote add origin https://useeman32-design:[GitHub-PAT-ASK-USER]@github.com/useeman32-design/deenapp.git`, user.name "DeenLink Dev", email dev@deenlink.org.
  * gh-pages local branch history diverged from origin -> force-push (content wiped+copied wholesale anyway). Deploy order: export -> cp dist/deenapp/. to /tmp/pages-out -> checkout gh-pages -> wipe (find . -mindepth 1 ! -name .git) -> cp pages-out -> commit/push (add .nojekyll! export-web.sh now creates it in dist).
- Pass-6 changes (all committed in 3b269be): index.tsx (image campaign banners 358x150 no text; section order CL→Daily Videos→Posts→Ayah→Hadith→Accounts; video modal reels-style w/ like/share/YouTube; ayah/hadith full-width stacked + modals w/ Share + module CTAs /tools/hadith + /(tabs)/quran; posts via FeedCard w/ dash palette + field chip POST_FIELDS {1 Aqeedah,2 Fiqh,3 Tajweed,4 Dhikr}; scholar photos scholar-{1,2,3}.jpg via SCHOLAR_AVATARS; sunpath Fajr=moon, Maghrib=custom SVG sunset glyph), FeedCard.tsx (dash/field props, color resolution), theme.ts (navTint dark 0.42 / light 0.45).
- Verified visually: VH=3500 dark+light shots (page now ~3500px tall — use n=5 slices), modal-test.mjs (scripts/) proves both modals open (VIDEO MODAL present: true / AYAH MODAL present: true).
- scripts/modal-test.mjs + scripts/dbg*.mjs + shot-slow.mjs are debug helpers (uncommitted except modal-test? check git status before next wipe).

# SESSION UPDATE 2026-08-27 ~14:20 (pass 7 SHIPPED)
- master = f7e3d59 "Home pass 7..." PUSHED; gh-pages = "Deploy pass 7" pushed. LIVE bundle entry-80febafaf7585149e4fc873fe43a9d70.js (200, 2.1MB; greps COMMENTS/Share as image/deenlink-daily/Finish the Qur/Add a comment/profiles all ≥1; p1 photo 200).
- Pass-7 changes: (1) campaign banners = image + app-rendered copy overlay on left (title/sub + gold chevron, LinearGradient scrim left→transparent, text width right:130); (2) nav glass FIXED FOR REAL: _layout outer View backgroundColor 'transparent' (was opaque d.navBg — that's why pass-6 "lighter tint" looked unchanged), removed invalid backdropFilter (tsc err), theme navTint dark 0.34 / light 0.40; (3) FeedCard full IG-style rewrite (42px photo avatar via AvatarImage helper, name+badge+time row, @handle + gold field chip ALWAYS shown (POST_FIELDS 101-104 → user.fields fallback from mocks u() opts), full body, Q&A card gold-accented, action row heart/comment-count/share/flag/•••, onComments prop); (4) CommentsModal.tsx NEW: bottom sheet, post summary, N COMMENTS, bubbles w/ photos+badges, like/Reply/"View N replies" nested (indent 26 + hairline), "Add a comment…" + Post (adds local comment); seeded from MOCK_COMMENTS (mocks.ts: SampleComment type + 101-104 data, nested replies, emoji); (5) ayah/hadith premium cards: gold inner frame + corner L-accent brackets + pattern bg (patternDark/Light) + gold eyebrow w/ icons+lines + Amiri-Bold 27 + diamond divider + italic meaning + REF · "Share as image"; (6) share card: src/lib/shareCard.ts (web canvas 1080×1350: icon.png logo, DEENLINK wordmark, eyebrow, Amiri Arabic RTL wrap, meaning, ref, QR via `qrcode` pkg createQR → modules.get, white box, "Scan to explore"); modal flow: Share Image btn → spinner → preview Image(dataURL) + Save (downloadDataUrl) + Share (shareOrSaveCard: Web Share file → fallback download); (7) content packs extracted to /tmp/packs (islamic: dua.json {English:[{ID,TITLE,AUDIO_URL remote mp3,TEXT[{ID,ARABIC_TEXT}]}]}, 99names.json {code,status,data:{names:[{number,name,transliteration,translation,meaning,audio:'/audio/asma-ul-husna/x.mp3'}]}}, seera_events_en.json 142, quiz_questions.json 148 {question,options,correct,explanation}; content: 114 quran/surah_N.json {surah,hasBasmallah,basmallah,verses[{ayah,arabic,english,hausa}]}, processed/*.json hadith (buhari 7277 {collection,chapter_number,chapter_name,arabic,...}), chapters_meta, profile/ web pages). 7 REAL profile photos pulled from live API search_accounts.php → assets/img/profiles/p1-p7.jpg, assigned to mock feed users (u() opts {photo,fields,badge,scholar}) + commenters. AUDIO WIRING = NEXT PASS (user: "note this, finish homepage first").
- Infra: scripts/browser-env.sh (recreates soname symlinks in /home/user/.chromium-libs + installs headless shell 1234 + /tmp/serve→dist) — run after every sandbox reset; modal-test2.mjs smoke test (port 8127): comments sheet + share card both PASS (dataURL 1.13MB). qrcode npm dep added + src/types/qrcode.d.ts. User type profile_image_url now string|number|null.
- LIVE site: https://useeman32-design.github.io/deenapp/ — pass 7 verified visually dark+light (VH=3700, n=6 slices) + modal screenshots.

## PASS 8 (shipped + live, 2026-08-27)
- master = 850dbdd "Home pass 8..." PUSHED; gh-pages = a817375 "Deploy pass 8" pushed.
- LIVE: https://useeman32-design.github.io/deenapp/ — bundle entry-75e0072b489c6f1a13e138d1736f3a08.js (200, 2.14MB).
- Pass-8 changes: (1) light nav glass fixed (theme light navTint 0.58 + _layout BlurView tint per-theme dark 42/light 22 — root cause was tint hardcoded "dark" + alpha 0.40); (2) iOS hero centering: index.tsx heroSz onLayout + exact {w,h} cover Image (removed transform scale(1.12)); (3) aqeedah chips: mocks fields 101 'Sunni · Mufti' / 102 'Sunni' / 103 'Sunni · Sheikh' / 104 'Sufi' + new posts 105 'Sufi', 106 'Sunni · Sheikh' (chip shows user.fields via FeedCard fieldLabel); (4) image post 105 (assets/img/post-mosque.jpg, golden mosque interior) + DOUBLE-TAP LIKE (lastTap ref <300ms, only if !liked, Animated heart burst scale 0.35→1.12, opacity interp [0,0.15,0.75,1]→[0,0.9,0.9,0] — initial 0.9 bug fixed); like count now reflects state: index passes like_count +1 when liked (togglePostLike only had a Set); (5) actions bigger (icons 21, text 14, gap 7, marginLeft 22); (6) YouTube post 106 (hwWpWoOtsBY Al-Baqarah Al-Shalabi) → YouTubeFrame (functional component returning real <iframe>, NOT class-extends-View — RNW View is not a class; tsc/TS2786) inside post container web-only; (7) Daily Videos real: 0R1LKPRwxR4 Surah Yasin (27:32), ta_tTZrarE0 Asma-ul-Husna (19:08), tlG38jgInLc Return to Allah (1:12) w/ source_url + embed_url; video modal renders YouTubeFrame on web (302×208) when embed_url; (8) ••• menu: Report post (Alert) + "Don't want to see this" (Alert Undo/OK → onDismiss → index setPosts filter).
- Verified headless (scripts/smoke8.mjs + dtap pattern, port 8128-8137, /tmp/serve = SYMLINK to dist — NEVER `rm -rf /tmp/serve/*`, it deletes the export!): video-modal iframe ✓, in-post iframe ✓, menu ✓, double-tap 128→129 ✓ (needed scrollIntoView — clicks at y>viewport are no-ops; elementFromPoint needs viewport coords), chips ✓, dark/light slices ✓.
- LIVE curl: entry 200; hwWpWoOtsBY/0R1LKPRwxR4/'Report post'/'want to see this'/'Mufti' (bundle escapes · as \xb7 — grep "Mufti" not "·")/post-mosque asset /deenapp/assets/assets/img/post-mosque.21181e....jpg 200 (double "assets" is the real path — earlier 404 was my sed/grep truncation again).
- INFRA: MID-TURN FULL RESET hit at 17:02 — wiped everything incl. src+node_modules, left only .git; recovered via `git checkout -f master` (all work was committed). THEN `find . -mindepth 1 ! -name .git -exec rm -rf` on gh-pages DELETED .git contents (find descends into .git) → recovered via fresh `git clone` (token URL in this file) + /tmp/pages-out. SAFE gh-pages wipe: `git ls-files -z | xargs -0 -r rm -rf --`. dist/ is volatile between calls (housekeeping) — keep export+test in ONE bash call; /tmp/serve→dist symlink means no re-copy needed.
- tsc quirk: `npx tsc` (wrong pkg) if node_modules wiped → `npm install` first (qrcode in package.json now).
- Open question for user: iOS "zoomed out" — only actionable lever was hero centering + layout; can't test native build here. Ask Expo Go vs EAS build if it persists.

## PASS 9 (shipped + live, 2026-08-27)
- master = 172ca91 "Home pass 9..." PUSHED; gh-pages = f2a7b98 "Deploy pass 9". LIVE bundle entry-b31706d2d15d94a009519beb16eb7ba0.js (200, 2.17MB); all markers grep-verified; vid-yasin asset 200.
- Changes: (1) SunPath night orb = two-circle crescent (old single-path crescent rendered hollow "empty circle"); (2) FeedCard v3: ••• menu = Report (→ full report modal: 6 types w/ icons + optional details + Submit) & Not interested (eye-slash, Alert Undo/OK → onDismiss); outer flag icon REMOVED; long text >230 chars → Show more/Show less; name+@handle numberOfLines=1 ellipsize tail; double-tap like on TEXT, IMAGE, YOUTUBE container (onTap w/ 300ms window + 310ms single-tap grace timer); image single tap → full-screen preview modal; burst overlay now on whole card; YouTube overlay Pressable: single tap → onPlayVideo (in-app player modal via index setVideoOpen), double tap → like; (3) CommentsModal v2: theme-aware (was hardcoded dark), HeartIcon filled + count increment, Reply → "Replying to {name}" gold bar + @handle prefilled in input + nested under parent + auto-open replies; (4) shareCard: SHARE_DESIGNS [classic vector / emerald / midnight / cream bg images] + dynamic canvas height (measure lines → H = max(1350, contentEnd+336)) + per-design ink colors (cream = dark text); CRITICAL: I broke rr() arcTo (wrote wrong corners → diagonal across card + clipped QR box) — restored pass-7 arcTo sequence; (5) index: home feed slice 6→8 (so YouTube post 106 stays visible after inserting long post 107), "Go to community" + new gold "View more on community" btn → /(tabs)/community, daily video tiles use v.thumb (assets vid-yasin/vid-99names/vid-return.jpg) + scrim; (6) COMMUNITY TAB replaces Learning in bottom bar (_layout TABS + Tabs.Screen name=community; learning.tsx MOVED to src/app/tools/learning.tsx — web replica screens are file-registered routes, so the leftover tab file created a 6th ghost "Home" tab item via TABS.find fallback); (7) community.tsx NEW: header, 3 stat cards (12.4k/312/58), composer (letter-A avatar, contenteditable placeholder "Share a thought, question or du'aa…", Post prepends local post), Community Posts (all MOCK_FEED via FeedCard), Recent Activity list (icon rows); (8) mocks: post 107 long reflection (after 101, in home slice), 108 question, 109 halaqah announcement; MOCK_COMMENTS 107/108/109; POST_FIELDS 107-109.
- Test gotchas: RNW TextInput renders contenteditable DIV not <input> (locator 'input[placeholder]' fails — use [placeholder*="..."]); in-app tab navigation WORKS (web) — earlier "broken" reading was innerText of hidden mounted screens (screens stay mounted, use visibility checks + screenshots); tab transition ~3s; Playwright locator.click on T-in-Pressable sometimes "intercepts pointer events" → dispatch MouseEvent via evaluate.
- User on Expo Go (iOS). Hero centering shipped; if still "zoomed out" in Expo Go → native build scale/safe-area territory, needs their next screenshot.

# SESSION UPDATE 2026-08-27 ~23:50 (PASS 10 SHIPPED & LIVE)
- master = d3d4be1 "Pass 10: …" (FORCE-pushed over 172ca91 — env reset had dropped local master to pass 8; d3d4be1 verified superset of origin/master: diff = only A/M, zero D, contains all pass-9 work + pass 10).
- gh-pages = 7ce97f80 (clean, 118 site files). NOTE: intermediate bbc6e04 accidentally committed node_modules+dist (36k files) because the gh-pages branch has NO .gitignore (master's is dropped on checkout) → `git add -A` staged everything. Fixed via `git rm -r --cached node_modules dist` + commit 7ce97f80. NEVER `git add -A` on gh-pages without that defensive unstage.
- LIVE verified: bundle entry-24a8cf64e0a82fd3f5b911dc7bcc39ee.js (200); markers Watch more / Search posts or accounts / TRENDING / #Tawbah / impactAsync / MOCK_PROFILES / Public Profile / Replying to all present; deep link /deenapp/profile/kunfai_ibrahim renders (404 shell + SPA = normal Pages behavior); 36/36 headless checks (scripts/smoke11.mjs) + visual reviews (profile w/ photo, comments w/ bold-emerald @mention + in-view Post button, video modal without Open in YouTube, composer w/ poll + Posting…, sticky tabs, poll vote 60%).
- All 13 pass-10 items done: community (stats cards removed, search posts+accounts, trending, sticky For you/Following/Scholars, FAB→composer w/ poll builder + "Posting…" progress), reply (no @handle prefill, "Replying to" chip, @mentions bold+emerald in text), 16px anti-zoom inputs, IG emoji rows, polls postable, public profile screen (photo/name/badges/fields, Posts/Followers/Following/Charity, Follow/Share, Posts/Questions/About tabs, answered questions), image preview fixed (explicit height 560 — RNW width-only+maxHeight rendered 0px), double-tap burst redesigned (back-easing pop 420ms + clean end-state, verified opacity 0, no shade), haptics (expo-haptics ~57.0.1, src/lib/haptics.ts, Platform-guarded), sun path continues after last prayer (fajr+(now-end) wrap), video modal (Open in YouTube removed, View all→Watch more).
- KEY BUGS FIXED THIS PASS: (1) decorative pattern Views/Images swallowed clicks → pointerEvents="none" on ALL (home hero, community, profile, onboarding, ayah/hadith card). (2) RNW horizontal ScrollView defaults flex:1 → emoji row stole 239px of comments sheet, clipped Reply buttons → height:36 + flexGrow:0 + flexShrink:0. (3) RNW TextInput won't shrink in flex row → Post label was at x=403 off-screen → width:0 + flex:1. (4) profile avatar: profile.photo IS the asset id (use directly, don't re-map via MOCK_PHOTOS).
- REPO IS useeman32-design/deenapp.git (user corrected: NOT deenlink-app). Token (ghp_…5ens) embedded in local remote URL; git identity repo-local (DeenLink Dev <dev@deenlink.org>).
- Test gotchas: expo-router Tabs keeps all screens in DOM (home first) → hit-test with elementFromPoint + in-viewport y, or you grab hidden copies (2 mosque imgs, 2 @alameen leaves); FA5 icons = text-glyph divs (no svg); headless rAF throttling lags animated DOM ~300ms (verify end-state, JS value is real-time); local server must serve dist UNDER /deenapp/ (bundle base path): /tmp/serve2/deenapp symlink, port 8152; kill server by PID (ss -ltnp), never pkill -f (matches own shell); video thumbs: center covered by play icon → click corner + accept ancestor containing 'Surah'/'27:32'.
- Env reset #4 wiped node_modules+dist+git remote+identity again mid-deploy (the checkout cycle deleted them from working tree once tracked on gh-pages). Restored: npm install + npm i --no-save playwright-core + export-web.sh + slashguard.
- Scripts kept: smoke11.mjs (36 checks), dbg15-34 (probes), shots10.mjs (final screenshots), livecheck.mjs (live deep-link render).

# SESSION UPDATE 2026-08-28 ~00:45 (env reset #5 recovery + deploy repaired + storage checked)
- ENV RESET #5 WIPED: .git/config (identity + token remote — .git/config is EXCLUDED from snapshots, happens EVERY reset), node_modules, dist, .cache (chromium), some /usr libs, and RESTORED A STALE .git STATE (origin/gh-pages ref pointed at an OLD pass-6/7 deploy commit, not the real remote tip!). LESSON: after ANY reset — (1) git config user.name/email, (2) git remote add origin https://useeman32-design:TOKEN@github.com/useeman32-design/deenapp.git, (3) ALWAYS git fetch before trusting local refs / deploying. TOKEN (user-supplied, needed after every reset): [GitHub-PAT-ASK-USER]
- User ran app on phone (Windows, C:\Projects\deenapp): git pull conflicted on community.tsx (force-push fallout) → fix given: `git fetch origin && git reset --hard origin/master && npx expo start --clear`.
- I BROKE the live deploy mid-session: orphan-commit deploy used the stale gh-pages ref → pushed an OLD pass-6/7 site tree (entry-75e0, learning.html, 109 files). REBUILT from source: npm install → export-web.sh → slashguard → orphan deploy. NEW GH-PAGES = 2 commits: "Deploy pass 10 (rebuilt from source)" + "Add expo asset fonts/images skipped by node_modules ignore rule" (117 files). CURRENT LIVE BUNDLE = **entry-ec722dfd7abf763d188e123d7eacdb7c.js** (200, all pass-10 markers, icon fonts 200, deep link /deenapp/profile/kunfai_ibrahim renders ✓).
- DEPLOY GOTCHA #2: repo .gitignore has `node_modules/` which matches at ANY depth → `git add -A` on the site silently skips the site's own `assets/node_modules/` (expo vector-icons FA fonts + expo-router images = 36 files; breaks all FA5 icons live) → ALWAYS `git add -f assets/node_modules` after copying site files.
- DEPLOY RECIPE v3 (safe): on master → export-web.sh + slashguard → cp dist/. /tmp/pages-out → `git checkout --orphan deploy-tmp` → `git rm -rq --cached .` → `git clean -fdq -e .gitignore` → `cp -r /tmp/pages-out/. .` → `git add -A` → `git add -f assets/node_modules` → commit → `git push -f origin deploy-tmp:gh-pages` → checkout master → branch -D. (git clean -fd does NOT touch ignored node_modules/dist; tracked .gitignore survives clean but is removed from index → it ends up committed on gh-pages, harmless/beneficial.)
- STORAGE CHECK: current in-scope workspace = ~83MB/379 files (65% of 128MB budget) — the 250.8MB "session created" warning was a TRANSIENT write peak (4 dist exports + gh-pages churn + repack), all transient files gone; "0 files not saved". In-scope big items: .git pack 37MB (18.4MB content-pack zip + 2MB slim zip + images + bundles in master history — legit, not removable without rewriting user's master history), 3 zips 20.8MB, images ~10MB, uploads 3.5MB, .chromium-libs 6.9MB. node_modules/dist/.cache are EXCLUDED from snapshots+budget. git gc --prune=now done; gh-pages remote history rewritten clean (no more 36k node_modules files on GitHub).
- CHROMIUM REINSTALL (after reset): `cd deenlink-app && npx playwright-core install chromium-headless-shell` (path unchanged: .cache/ms-playwright/chromium_headless_shell-1234/…) + apt libs copied into .chromium-libs: libatk1.0-0 libatk-bridge2.0-0 libxdamage1 libxkbcommon0 libasound2 libatspi2.0-0 (plus the older set already there). livecheck.mjs committed to master (scripts/).
- master = 6807640 (d3d4be1 + livecheck commit), pushed. Local refs: only master + origin/* (deleted local gh-pages).

# SESSION UPDATE 2026-08-28 ~02:30 (PASS 11 SHIPPED — native fixes + mock-only mode)
- **RESET PATTERN CONFIRMED: the env resets at EVERY user-turn boundary** — wipes node_modules, dist, .cache (chromium), /usr apt libs, .git/config (identity+token remote). /home/user files persist (source, .git objects, .chromium-libs, /tmp usually). Per-turn restore ritual: (1) git config user.name "DeenLink Dev" + user.email dev@deenlink.org, (2) git remote add origin https://useeman32-design:[GitHub-PAT-ASK-USER]@github.com/useeman32-design/deenapp.git, (3) npm install + npm i --no-save playwright-core, (4) npx playwright-core install chromium-headless-shell, (5) re-start 8152 server if needed.
- **PASS 11 (master b44d246, pushed; gh-pages = "Deploy pass 11" 117 files; LIVE bundle entry-4c9d9251adc771c97d9c0362c48f9d58.js):**
  1. **FORCE_DEMO = true in src/api/client.ts** — mock-only mode: request() short-circuits to networkError (ALL existing fallbacks → mocks); restoreSession → MOCK_USER; updateProfile → {ok:true, user:{...MOCK_USER,...payload}, 'Saved (demo mode)'}; dailyCheckin → {ok:true, points:1}. login/register work via existing demo path (AuthContext: res.demo → MOCK_USER w/ prettyName). Flip FORCE_DEMO=false to go live. App is now DEMO-ONLY on web+native.
  2. **MOCK_USER.profile_image_url = p1** (demo user now has a real photo asset).
  3. **NATIVE CRASH FIXED (community tab "View config getter callback for component `b`")**: ACTIVITY list used HTML `<b>` tags — react-native-web tolerates unknown lowercase tags (renders div), NATIVE CRASHES. Replaced with `const B = ({children}) => <Text style={{fontWeight:'800'}}>{children}</Text>`. LESSON: grep src for HTML tags before shipping native — web passing ≠ native safe.
  4. **VideoModal.tsx (shared component)** — extracted from index.tsx; props {video, liked, onLike, onClose}. Web: YouTube iframe (embed_url). Native: player-style 302×170 rounded preview with real thumbnail (video.thumb asset number OR poster_url uri) + dark overlay + emerald play button + duration chip + explicit gold "Watch on YouTube" button (Linking). Wired to BOTH home (replaces inline modal) AND community (new videoPost state + onPlayVideo on FeedCard; like state = likedPosts set). EXPORTS fmtViews (index.tsx keeps its local copy — harmless dup).
  5. HONEST LIMIT (told user): Expo Go CANNOT embed a live YouTube player (react-native-webview not in Expo Go). True in-app iframe needs a dev build. Native gets player-style preview + explicit handoff to YouTube app.
- User's phone (C:\Projects\deenapp, Windows, Expo Go): told to `git fetch origin && git reset --hard origin/master && npx expo start --clear` — again after pass 11 push.
- Native complaints that FORCE_DEMO fixes: "logging in with real info" (→ demo profile w/ photo), "comments not showing" (live posts had no mock comment mapping; mocks have MOCK_COMMENTS fallback to 101), "profile photo not showing" (live user's remote URL broken; now local asset p1), "some posts loading" (live API partial responses; now full mock feed 101-110).
- tsc: use `./node_modules/.bin/tsc --noEmit` (npx tsc grabs fake tsc@2.0.4 pkg).
- Deploy recipe v3 WORKED again (orphan + git rm --cached . + clean -fdq -e .gitignore + cp pages-out + add -A + add -f assets/node_modules + force-push).

# SESSION UPDATE 2026-08-28 ~03:00 (handoff created — chat is long, storage is fine)
- VERDICT on "chat heavy/slow": STORAGE IS FINE — 86.4 MB / 393 files in-scope (67% of 128MB budget, 4% of 10k files). Breakdown: .git pack 36.8MB (legit history, incl. 18.4MB content-pack zip), zips 17.6MB+1.9MB+0.3MB, images ~10MB, .chromium-libs ~12MB, uploads 3.5MB. Cause = long conversation context + per-turn 90MB snapshot. Fix = new agent chat.
- Created **CONTINUE.md at repo root** (master a4270e8 → ef88edb): full handoff (state, user style, standing requirements, code mechanics, test setup, deploy recipe v3, sandbox reset ritual + token note (ask user; repo is public — token must NOT go in repo), storage audit, pass history 1-11, next likely asks). Points to /home/user/.session-memory.md for the chronological log.
- Told user: start a fresh chat, first message = "Continue the DeenLink project — read CONTINUE.md in the workspace".
- Optional storage optimization if ever needed: rewrite master history to drop deenlink-content-pack.zip (~18MB) — invasive (user re-clone), don't do without asking.


# SESSION UPDATE 2026-08-28 ~13:00 (PASS 12 built — assets, auth redesign, reels, community fixes)
- Scope (user's unsent prompt from old chat + attachments): image diet, use/wipe zips, pro onboarding both themes, login/signup redesign from his 3 images (design + light bg + dark bg — OCR'd: DeenLink / Strengthen Your Deen, Every Day / Welcome back! / Email+Password / Forgot / Sign In / OR / G Sign in with Google / Sign Up link), bottom menu too high, TikTok-style videos page (like/comment/save/share/download, 3 sample videos, profile About→Videos), community: composer back on top + FAB off the nav + pro poll w/ changeable vote + comments sheet closes on profile + only avatar/name tappable + thread line redesign. Videos placement decision: Quick Access first button + campaign banner (5 tabs stay).
- ASSETS: scripts/slim-assets.py committed (jpg q80 maxw1200; png FASTOCTREE/MEDIANCUT quantize; onboard png→jpg). img 12M→3.6M, images 1.4M→364K. Verified lossless vs git HEAD (icon MAD 2.57, patterns 0.36, glow 2.48). auth-bg-dark/light.jpg from uploads (780x1686 q80, 144K/113K).
- ZIPS: staged content/ (quran 114 surah + ayah_search_index + islamic 4 jsons = 8.3M, gitignored via `content/` line in .gitignore — python zipfile needed for backslash paths `quran\surah_N.json`). Deleted all 3 zips; originals in git history.
- REELS: expo-video + expo-media-library + expo-file-system (`npx expo install` — all Expo Go compatible; NOTE package is expo-file-SYSTEM not filesystem). Mixkit assets URLs pattern `https://assets.mixkit.co/videos/{id}/{id}-{720|360}.mp4` — Cloudflare-safe via curl; Pexels/Pixabay pages are JS-walled. No portrait Islamic footage → ffmpeg center-crop 16:9→9:16 (`crop=ih*9/16:ih,scale=540:960`, crf 27, -an, +faststart): f1=11922 drone mosque 953K, f2=34208 Quran reading 615K (luma 182 bright), f3=39179 interior 2.1M. playwright ffmpeg build CANNOT demux mp4 → apt install ffmpeg (7.1). Posters/banner: ffmpeg -ss N -frames:v 1.
- AUTH REDESIGN: AuthShell.tsx (AuthShell/AuthHeading/AuthField/AuthPrimaryButton/AuthGoogleButton/AuthOrDivider/AuthSwitchLine), login.tsx + register.tsx rewritten (register: name/email/password + aqeedah chips + Terms line), onboarding.tsx restyled (dash tokens, W=Dimensions, STATS card, Skip, dots, framed jpgs). FontAwesome5 `brand` prop for google icon.
- **CRITICAL demo-session change**: client.ts restoreSession FORCE_DEMO branch now returns MOCK_USER only if storage 'dl.demoSession'==='1' (else null → login shows once). login/register demo paths set the flag; logout clears it. All test seeds MUST include dl.demoSession=1 (smoke12 addInitScript does).
- VIDEOS: src/app/videos.tsx (root Stack.Screen presentation fullScreenModal animation slide_from_bottom in _layout.tsx). FlatList snapToInterval=VH getItemLayout; ReelItem mounts VideoView ONLY when active (others = poster Image) — memory-safe for 3+ reels. useVideoPlayer(src=require) p.loop/muted; progress via 400ms interval currentTime/duration. Tap=play/pause center icon; double-tap=like burst (reuse pass-10 pattern). Download: web expo-asset Asset.fromModule(reel.src).uri + <a download>; native downloadAsync + MediaLibrary.requestPermissionsAsync + saveToLibraryAsync(localUri). CommentsModal reuse via reelAsPost() synthetic Post.
- COMMUNITY: composer bar (avatar + placeholder + poll/camera chips) above search, opens existing modal; FAB bottom insets+96 (was +78 = on the nav); poll rewritten (vote/change/retract + LayoutAnimation + radio/check + fills); comments openProfile = onClose + 140ms delay push; avatar+name only tappable; replies in borderLeft emerald rail (borderTopLeftRadius 8) — no absolute hairline. Nav bar bottom 20→8+insets.
- WIRING: quick-access videos entry href '/videos' icon play-circle; DEFAULT_QUICK videos FIRST; QUICK_STORAGE_KEY → 'dl.quickaccess.v2' (bump = everyone gets new defaults once); QuickGrid videos href; home CAMPAIGNS new first entry (campaignVideos img, 'DeenLink Videos'); Watch more → /videos; tools/videos.tsx = Redirect.
- PROFILE: ProfileTab 'about'→'videos'; TABS label About→Videos; About cards REMOVED, replaced by poster grid (3-col, (W-44)/3, tap → /videos?start=id); userReels = MOCK_REELS.filter(username).
- TESTING GOTCHAS THIS PASS: direct URL loads with .html extension (e.g. /(auth)/login.html) = "Unmatched Route" — ALWAYS navigate in-app (tab clicks / Quick Access chips) or seed storage via ctx.addInitScript + fresh goto; python http.server has NO extensionless fallback; RNW nested ScrollView scrollIntoView scrolls the WRONG container on community (use search-results navigation for profile, or scrollTop on the found scroller); poll clicks need scrollIntoView + click at x+20 (row LEFT, text area); deviceScaleFactor 2 contexts fine; clearing localStorage on about:blank throws SecurityError (goto first).
- SMOKE: scripts/smoke12.mjs = 36 legacy + 11 new = 47 checks, ALL PASS. New: home campaign banner, quick-access Videos first (30,501), videos feed renders + <video> 390x844 + rail (1,284/96/Save/Share/nasheed) + double-tap like 1,285, login headings/fields/switch, composer bar, poll vote+change (291 votes once, switch works).
- Shots for user review: /home/user/shots-pass12/*.png (01-02 onboarding, 03-04 login/register dark+light, 05-06 videos f1, 07-08 community+poll, 09b/09 profile+Videos tab).
- .gitignore additions: `content/`. Deploy: NOT YET DONE at time of writing (needs token + user verify).


# SESSION UPDATE 2026-08-28 ~14:30 (PASS 13 — videos app features + community polish)
- iOS VIDEO PLAYED IN SYSTEM PLAYER: expo-video VideoView `nativeControls` DEFAULTS TRUE on iOS → tap = Apple chrome/fullscreen. FIX: nativeControls={false} + VideoView wrapped in View pointerEvents="none" (all input via our overlay).
- Videos: Following/For you tabs (Following = MOCK_FOLLOWED filter — added reels 204 @aisha_yusuf f4 clouds 280K, 205 @Gimba f5 dawn 280K from mixkit 9736/9998 so tabs differ); search overlay (placeholder-element check, NOT bodyText — placeholders aren't innerText); library sheet (bookmark icon \uf02e) = Saved/Liked/Reposts poster grids (persist dl.reels.saved + dl.reels.reposted); repost rail button \uf021 label 'Repost' + toast 1.8s; rail download label 'Save'→'Download' (was ambiguous with bookmark); create studio = bottom sheet: Choose video (web hidden <input type=file> + URL.createObjectURL; native expo-image-picker ['videos'] — installed, Expo Go ok) OR sample clips; posts via src/lib/reelStore.ts (module store, subscribe) → feed + community both render. Created reel user 'abdalrahman'.
- Community: composer UNDER search (expanded 42px avatar + POLL·VIDEO·YOUTUBE hint + film chip → /videos?create=1); trending pills gap moved to contentContainerStyle (gap on ScrollView style is IGNORED — that's why they were cramped) + paddingVertical; sticky tabs paddingTop Math.max(insets.top,12) + paddingBottom 10; poll duration chips 1h/8h/1d/3d/7d → PostPoll.duration (types.ts) shown as 'ends in X'; composer Video chip → /videos?create=1, YouTube chip → URL input → post youtube_url + parsed embed (regex v=|youtu.be|embed|shorts).
- reelStore → community subscription: new reels prepend posts id 900000+reel.id with video:{reelId,poster}; FeedCard renders video card (poster h300 + DEENLINK VIDEO chip) → /videos?start=reelId.
- TEST GOTCHAS: suite must goBack() off /videos before tab-bar lookups; close sheets by CLICKING BACKDROP (195,150) not computed ✕ points; leave modal state clean or later blocks hit backdrops; search assertion needs a REAL term ('dhikr' matches reel 204, 'quran' matches nothing); rail bookmark = count label click (312), NOT a text button.
- smoke13.mjs = 62 checks ALL PASS. Shots: /home/user/shots-pass13/.
